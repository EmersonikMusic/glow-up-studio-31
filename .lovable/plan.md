Update the Sign in with Apple button in `src/components/AuthModal.tsx` so it uses the same border color as the Google button (`#8E918F`) instead of the current black border (`#000`). This aligns both social pills visually while keeping their distinct fill colors and remaining brand-compliant. No other changes needed.