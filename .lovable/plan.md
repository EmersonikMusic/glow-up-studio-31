Replace the text "Want to just eventually know everything? We got you." in `src/components/AboutScreen.tsx` with "Want to just eventually know everything? Start by knowing that we got you."

That's the only change.