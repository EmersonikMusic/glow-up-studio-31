Update `src/components/PauseOverlay.tsx`:

1. Reduce "Paused" heading size (from `text-5xl sm:text-6xl` to `text-3xl sm:text-4xl`) and change text to uppercase ("PAUSED").
2. Change bottom helper text from "Press space or start button to resume" to "PRESS SPACE OR PLAY BUTTON TO RESUME" (already uppercase styled via class, just update the literal).

No other styling, layout, or behavior changes.